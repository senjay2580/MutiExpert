# 部署诊断

系统化分析项目的部署状态和环境配置，生成详细的诊断报告。

## 功能说明

自动收集和分析：
- 服务器信息（系统资源、Docker状态）
- 容器状态（运行中的容器、端口映射、网络配置）
- 数据库连接（DM8、Redis）
- 前后端配置（API地址、环境变量、Nginx配置）
- 配置一致性检查（文档 vs 代码 vs 实际运行状态）

## 参数

- `$ARGUMENTS` - 可选参数：
  - 空参数 - 执行完整诊断（默认）
  - `quick` - 快速诊断（仅本地配置和服务器基本信息）
  - `server` - 仅服务器诊断
  - `config` - 仅配置文件分析

## 执行步骤

### 1. 准备阶段

读取项目文档和凭证：
- 读取 `账号密码.md` 获取服务器凭证
- 读取 `.claude/commands/deploy.md` 了解部署架构
- 确认工作目录：`d:\Desktop\MySystem\4.门店管理系统\store_manager`

**关键信息提取**：
- 服务器 IP: `118.31.246.183`
- 用户名: `root`
- 密码: `RbZnH#DqH7csm5DRjFNvkkpjtuv%cG`
- DM8 数据库: `47.113.200.12:30236`
- Schema: `GOLDSTORE`
- 数据库密码: `D3MFelvccMW3Am2Y`

### 2. 本地配置收集

#### 前端配置分析
读取以下文件并提取关键信息：
- `store_manager_admin/config/apiConfig.ts` - API基础路径配置
- `store_manager_admin/config/env.ts` - 环境配置和开关
- `store_manager_admin/nginx.conf` - Nginx代理配置
- `store_manager_admin/package.json` - 项目版本和依赖

**提取信息**：
- API配置：development、production 的值
- USE_LOCAL_API 开关状态
- Nginx 代理规则
- 容器名称和端口映射

#### 后端配置分析
读取以下文件并提取关键信息：
- `store_manager_api/src/main/resources/application.yml` - 主配置
- `store_manager_api/src/main/resources/application-prod.yml` - 生产配置（如果存在）
- `store_manager_api/pom.xml` - 项目版本和依赖

**提取信息**：
- 服务端口：8080
- 数据库连接：URL、用户名、Schema
- Redis配置：host、port
- JPA配置：ddl-auto、方言
- JWT配置：secret、过期时间

### 3. 远程服务器诊断

使用 Python paramiko 连接服务器执行诊断命令（不创建临时文件）。

**重要**：使用 PowerShell 执行，确保 UTF-8 编码：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
python -c "
import paramiko
import json

# 连接服务器
client = paramiko.SSHClient()
client.set_missing_host_key_policy(paramiko.AutoAddPolicy())
client.connect('118.31.246.183', 22, 'root', 'RbZnH#DqH7csm5DRjFNvkkpjtuv%cG', timeout=10)

# 收集系统信息
commands = {
    'system': 'uname -a',
    'uptime': 'uptime',
    'disk': 'df -h /',
    'memory': 'free -h',
    'docker_version': 'docker --version',
    'docker_ps': 'docker ps --format \"{{.Names}}\t{{.Status}}\t{{.Ports}}\"',
    'docker_images': 'docker images --format \"{{.Repository}}:{{.Tag}}\t{{.Size}}\"',
    'docker_networks': 'docker network ls',
    'processes': 'ps aux | grep -E \"java|nginx|redis\" | grep -v grep',
    'ports': 'netstat -tlnp | grep -E \":(80|443|7000|8080|6379|5236)\"'
}

results = {}
for key, cmd in commands.items():
    stdin, stdout, stderr = client.exec_command(cmd)
    results[key] = stdout.read().decode('utf-8', errors='ignore')

# 获取容器详细信息
stdin, stdout, stderr = client.exec_command('docker ps -q')
container_ids = stdout.read().decode('utf-8').strip().split('\n')

containers = []
for cid in container_ids:
    if cid:
        stdin, stdout, stderr = client.exec_command(f'docker inspect {cid}')
        containers.append(stdout.read().decode('utf-8'))

results['containers'] = containers

# 输出 JSON 格式结果
print(json.dumps(results, ensure_ascii=False, indent=2))

client.close()
"
```

**收集的信息**：
- 系统信息：内核版本、运行时间
- 资源使用：磁盘、内存
- Docker 状态：版本、运行中的容器、镜像列表、网络
- 容器详情：每个容器的完整配置（端口映射、环境变量、网络、挂载）
- 进程信息：Java、Nginx、Redis 进程
- 端口监听：80、443、7000、8080、6379、5236

### 4. 数据库连接测试

测试数据库连接可用性：

```powershell
python -c "
import socket

# 测试 DM8 数据库
def test_connection(host, port, name):
    try:
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.settimeout(3)
        result = sock.connect_ex((host, port))
        sock.close()
        if result == 0:
            print(f'✅ {name} ({host}:{port}) - 连接成功')
        else:
            print(f'❌ {name} ({host}:{port}) - 连接失败')
    except Exception as e:
        print(f'❌ {name} ({host}:{port}) - 错误: {e}')

test_connection('47.113.200.12', 30236, 'DM8 数据库')
test_connection('118.31.246.183', 6379, 'Redis (生产服务器)')
test_connection('localhost', 6379, 'Redis (本地)')
"
```

### 5. 配置一致性检查

对比以下配置，检测不一致：

#### 检查项 1：API 配置
- ✅ **正确**：`apiConfig.ts` 的 `production` 值为空字符串 `''` 或使用域名
- ❌ **错误**：硬编码 IP 和端口（如 `http://118.31.246.183:7000`）

#### 检查项 2：容器状态
- ✅ **正常**：`deploy.md` 中列出的容器都在运行
- ⚠️ **警告**：有容器未运行或状态异常
- ❌ **错误**：关键容器（store_manager_api、store_manager_admin）未运行

#### 检查项 3：端口映射
- ✅ **一致**：实际端口映射与 `deploy.md` 文档一致
- ⚠️ **不一致**：端口映射与文档不符

#### 检查项 4：数据库配置
- ✅ **正确**：Schema 名称为大写 `GOLDSTORE`
- ❌ **错误**：Schema 名称为小写或错误

#### 检查项 5：资源使用
- ✅ **正常**：磁盘使用 < 80%，内存使用 < 90%
- ⚠️ **警告**：磁盘使用 80-90%，内存使用 90-95%
- ❌ **危险**：磁盘使用 > 90%，内存使用 > 95%

### 6. 生成诊断报告

生成 Markdown 格式的报告文件：`部署诊断报告_YYYYMMDD_HHMMSS.md`

**报告结构**：

```markdown
# 部署诊断报告

**生成时间**：2026-02-05 14:30:00
**项目**：黄金门店CRM管理系统
**诊断范围**：完整诊断

---

## 📊 概览

| 项目 | 状态 | 说明 |
|------|------|------|
| 服务器连接 | ✅ 正常 | 118.31.246.183 |
| 容器运行 | ✅ 正常 | 2/2 容器运行中 |
| 数据库连接 | ✅ 正常 | DM8 可访问 |
| 配置一致性 | ⚠️ 警告 | 发现 1 个配置问题 |
| 资源使用 | ✅ 正常 | 磁盘 8%, 内存 65% |

**诊断结果**：发现 1 个警告，0 个错误

---

## 🖥️ 服务器信息

### 基本信息
| 配置项 | 值 |
|--------|-----|
| 公网 IP | 118.31.246.183 |
| 系统 | Ubuntu 22.04.5 LTS |
| 内核 | 5.15.0-164-generic |
| 架构 | x86_64 |
| 运行时间 | 15 天 3 小时 |

### 资源使用
| 资源 | 使用情况 | 状态 |
|------|----------|------|
| 磁盘 | 2.7G / 40G (8%) | ✅ 正常 |
| 内存 | 1.0G / 1.6G (65%) | ✅ 正常 |

### Docker 信息
- Docker 版本：24.0.7
- 运行中容器：2 个
- 镜像数量：5 个
- 网络：apinet (bridge)

---

## 🐳 容器状态

### 运行中的容器

| 容器名 | 状态 | 端口映射 | 镜像 |
|--------|------|----------|------|
| store_manager_api | Up 5 days | 7000:8080 | registry.cn-hangzhou.aliyuncs.com/gemini-hwf/store-api:latest |
| store_manager_admin | Up 5 days | 80:80, 443:443 | registry.cn-hangzhou.aliyuncs.com/gemini-hwf/store-admin:latest |

### 容器详细配置

#### store_manager_api
- **网络**：apinet (172.18.0.3)
- **环境变量**：
  - SPRING_PROFILES_ACTIVE=prod
  - TZ=Asia/Shanghai
- **挂载**：无
- **重启策略**：always

#### store_manager_admin
- **网络**：apinet (172.18.0.2)
- **环境变量**：
  - TZ=Asia/Shanghai
- **挂载**：无
- **重启策略**：always

---

## 🔧 配置信息

### 前端配置 (store_manager_admin)

#### API 配置 (config/apiConfig.ts)
```typescript
development: "http://localhost:8080"  // 或 "https://api-goldstore.gemini-galaxy.com"
production: "https://api-goldstore.gemini-galaxy.com"
```

**状态**：✅ 正确（使用域名，不是硬编码IP）

#### 环境配置 (config/env.ts)
- USE_LOCAL_API: true (开发环境使用本地API)
- 本地 API: http://localhost:8080
- 生产 API: https://api-goldstore.gemini-galaxy.com

#### Nginx 配置
- 监听端口：80, 443
- SSL 证书：已配置
- API 代理：/api/ -> http://store_manager_api:8080/api/
- 静态文件：/usr/share/nginx/html

### 后端配置 (store_manager_api)

#### 应用配置 (application.yml)
- 服务端口：8080
- 应用名称：gemini-api
- 上下文路径：/

#### 数据库配置
- 类型：DM8 达梦数据库
- 地址：jdbc:dm://47.113.200.12:30236?schema=GOLDSTORE
- Schema：GOLDSTORE ✅ (大写，正确)
- 连接池：Druid (初始5, 最大20)
- 方言：OracleDialect

#### Redis 配置
- 地址：localhost:6379
- 数据库：0
- 连接池：最大8个连接

#### JPA 配置
- DDL 模式：update
- 显示 SQL：true
- 命名策略：PhysicalNamingStrategyStandardImpl

---

## 🔍 数据库连接测试

| 数据库 | 地址 | 状态 |
|--------|------|------|
| DM8 数据库 | 47.113.200.12:30236 | ✅ 连接成功 |
| Redis (生产) | 118.31.246.183:6379 | ❌ 连接失败 (端口未开放) |
| Redis (本地) | localhost:6379 | ✅ 连接成功 |

**说明**：生产服务器 Redis 端口未对外开放，容器内部通过 Docker 网络访问。

---

## ⚠️ 问题列表

### 警告 (1)

1. **Redis 外部访问**
   - **问题**：生产服务器 Redis 端口 6379 无法从外部访问
   - **影响**：无法从本地直接连接生产 Redis（这是正常的安全配置）
   - **建议**：保持现状，容器内部通过 Docker 网络访问即可

### 错误 (0)

无错误。

---

## 💡 建议

### 配置优化
1. ✅ API 配置正确，生产环境使用域名而非硬编码 IP
2. ✅ 数据库 Schema 名称使用大写 `GOLDSTORE`，符合 DM8 规范
3. ✅ 容器配置了自动重启策略 `always`

### 安全建议
1. ✅ 服务器仅开放 22 端口，所有 HTTP 请求通过 Nginx 代理
2. ✅ Redis 端口未对外开放，仅容器内部访问
3. ⚠️ 建议定期更新 SSL 证书（检查到期时间）

### 运维建议
1. 磁盘使用率 8%，空间充足
2. 内存使用率 65%，建议监控 Java 进程内存
3. 容器运行稳定（已运行 5 天），无需重启

---

## 📝 附录

### 访问地址
- 前端：https://admin-goldstore.gemini-galaxy.com
- 后端 API：https://admin-goldstore.gemini-galaxy.com/api
- Swagger：http://118.31.246.183:7000/swagger-ui/index.html

### 凭证位置
- 服务器凭证：`账号密码.md`
- 数据库凭证：`账号密码.md` 和 `application.yml`
- Docker 仓库：`账号密码.md`

### 相关文档
- 部署流程：`.claude/commands/deploy.md`
- Docker 规范：`Docker部署规范.md`
- 调试记录：`调试记录_*.md`

---

**报告结束**
```

### 7. 输出报告

将生成的报告保存到项目根目录，并告知用户：
- 报告文件路径
- 发现的问题数量（错误、警告）
- 关键发现摘要
- 建议的后续操作

## 技术细节

### Windows PowerShell UTF-8 编码

所有命令执行前设置编码：
```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
```

### Python paramiko 不创建文件

使用 `python -c "..."` 直接执行，不创建 `.py` 文件：
```powershell
python -c "
import paramiko
# ... 代码 ...
"
```

### 凭证读取规范

从 `账号密码.md` 读取真实凭证，不使用占位符：
- 服务器密码：`RbZnH#DqH7csm5DRjFNvkkpjtuv%cG`
- 数据库密码：`D3MFelvccMW3Am2Y`
- Docker 仓库密码：`hwf994437346`

### 错误处理

- 服务器连接失败：报告错误，跳过远程诊断，继续本地配置分析
- 文件读取失败：记录缺失的文件，继续其他检查
- 命令执行超时：设置 10 秒超时，超时则标记为失败

## 注意事项

### 安全
- ⚠️ 报告中不包含完整密码，仅显示 `***` 或引用文件位置
- ⚠️ 报告文件不提交到 Git（已在 .gitignore 中）

### 性能
- 完整诊断约需 30-60 秒
- 快速诊断约需 10-20 秒
- 使用 `quick` 参数可加快速度

### 兼容性
- 仅支持 Windows 11 + PowerShell
- 需要安装 Python 3.x 和 paramiko 库
- 需要网络连接到生产服务器

## 常见问题

### paramiko 未安装
```powershell
pip install paramiko
```

### 连接超时
- 检查网络连接
- 检查服务器 IP 和端口
- 检查防火墙设置

### 编码错误
- 确保 PowerShell 使用 UTF-8 编码
- 使用 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`

### 权限不足
- 确保有读取配置文件的权限
- 确保有写入报告文件的权限
