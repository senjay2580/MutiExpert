# Skill Registry — 技能注册表

> 按角色（Agent）组织 Skills。编排器根据子任务类型分配角色，角色自带技能包。
> 来源：项目命令 > 全局命令 > 在线搜索

### Skill 加载规则

本注册表为只读，不可修改。

**全局 Skill**（来源标记为「全局」）：
1. 先查项目 `.claude/commands/` → 存在**同名**则从项目路径读取（软连接覆盖）
2. 项目下不存在 → 从全局 `C:\Users\33813\.claude\commands\` 读取
3. 全局也不存在 → 在线搜索

**项目级 Skill**（来源标记为「项目级」）：

1. 直接从当前项目 `{项目根目录}/.claude/commands/` 读取
2. 项目下不存在 → 不回退全局，提示用户当前项目缺少该 skill 

---

## @researcher — 调研员

> 需求对齐、技术调研、信息搜索、资源发现、方案评估。需求模糊时首先激活。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/web-search-plus` | 全局 | 8引擎智能路由搜索（Serper/Tavily/Exa/You/SearXNG/GitHub/Reddit/Twitter）。自动分析query选最佳provider，支持中英文预处理、并行子搜索、视频资源扩展、结果质量过滤+去重。兜底策略：脚本失败自动切内置WebSearch |
| `/search-skill` | 全局 | 多源AI Skill搜索。SkillsMP语义搜索(10000+ skills)+关键词精确匹配+GitHub仓库搜索。4种模式：auto/keyword/github/all。中文意图自动翻译英文关键词 |
| `/requirement-alignment` | 全局 | 需求对齐引擎(opus)。接收模糊需求→多层级代码定位(精确到行)→需求工程方法论(Karl Wiegers/Robertson/Cockburn)→结构化复述确认。覆盖bug/新功能/增强/重构四类需求 |
| `/solution-evaluator` | 全局 | 技术方案评估(opus)。基于ADR+Trade-off分析+第一性原理+MECE原则，评判现有方案优劣，提出更优方案，展示完整推导链路 |
| `/blog-digest` | 全局 | 博客/文章/论坛摘要。从URL/文本提取关键观点+可执行步骤+风险，分析评论/回复，输出结构化Markdown摘要+链接引用表 |

**触发词**: 搜索、查找、调研、找skill、需求对齐、需求不清楚、方案评估、评判方案、摘要、总结文章

**内部执行策略**:
```
1. 需求模糊？ → /requirement-alignment 先对齐需求
2. 需要外部信息？ → /web-search-plus 搜索（技术方案/文档/最新动态）
3. 搜索结果含长文？ → /blog-digest 提取摘要
4. 需要找现成工具/skill？ → /search-skill 搜索市场
5. 多个方案需要对比？ → /solution-evaluator 评估优劣
规则：按需组合，不必全部调用。有明确需求跳过步骤1，无需外部信息跳过步骤2-3。
```

---

## @architect — 架构师

> 系统设计、模块拆分、流程可视化、业务流分析、项目经验提取。完整流必经角色。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/excalidraw-diagram-generator` | 全局 | 自然语言→Excalidraw图表。支持9种图：流程图/关系图/思维导图/架构图/DFD/泳道图/类图/时序图/ER图。输出.excalidraw JSON，含Python脚本批量添加图标和箭头 |
| `/draw-io` | 全局 | draw.io XML图表创建/编辑/PNG转换。支持坐标精确调整、字体配置、AWS图标、布局规范。含转换脚本和质量检查清单 |
| `/business-flow-analyzer` | 全局 | 业务流深度分析(opus)。基于DDD/BPMN2.0/C4模型/金字塔原理，分析业务逻辑生成8维技术文档：数据模型/流程图/执行示例/数据变更/业务规则/算法/边界处理/错误机制 |
| `/analyze-project` | 全局 | 项目经验提取(sonnet)。基于ADR/Kleppmann三维度/Cagan问题-方案分离/Google无责复盘/Bezos决策框架。4种模式：retrospect(复盘)/reference(参考)/template(模板)/compare(对比) |
| `/test-driven-development` | 全局 | 🔒 **TDD模式专属** TDD铁律(obra/superpowers)。Red-Green-Refactor严格循环，没有失败测试就不写生产代码。含反模式识别、常见借口反驳表、验证清单。违反规则=删代码重来 |
| `/tdd-orchestrator` | 全局 | 🔒 **TDD模式专属** TDD多agent编排器(sickn33/antigravity)。协调多个agent执行Red-Green-Refactor，用Opus模型做架构决策，自动分配测试任务到subagent并行执行 |

**触发词**: 架构设计、画流程图、画架构图、模块拆分、方案对比、技术选型、可视化、业务流分析、项目复盘

**内部执行策略**:
```
1. 有现有项目要分析？ → /analyze-project 提取架构经验
2. 涉及业务流程？ → /business-flow-analyzer 生成8维技术文档
3. 需要可视化产出？ → 简单图用 /draw-io，复杂交互图用 /excalidraw-diagram-generator
4. 多个方案？ → 先画各方案架构图，再交给 @reviewer 评审
5. 🔒 TDD模式下 → /test-driven-development 定规则 + /tdd-orchestrator 编排多agent执行
规则：分析先行(1-2)，可视化跟进(3)。TDD skill 仅在用户指定 TDD 模式时加载。
```

---

## @designer — 设计师

> 视觉设计、海报、主题风格。纯视觉产出（非前端编码）时激活。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/canvas-design` | 全局 | 原创视觉艺术设计(PNG/PDF)。两步流程：①创建设计哲学宣言→②在画布上表达(90%视觉+10%文字)。博物馆/杂志级工艺品质，支持多页PDF |
| `/theme-factory` | 全局 | 主题工厂。10个预设专业主题，每个含完整色板(hex)+字体配对。可套用到幻灯片/文档/报告/HTML页面等任何产出物 |
| `/placeholder-image` | 全局 | 免费占位图获取。原型阶段需要临时图片时使用 |
| `/UIUX设计增强` | 全局 | UI/UX设计灵感参考。参照Behance/Mobbin/观猹/超级设计等平台+GitHub知名项目，追求交互感、用户体验感、美观现代化舒适感 |

**触发词**: 海报、视觉设计、配色、主题、美化、设计灵感、艺术品

**内部执行策略**:
```
1. 需要设计灵感？ → /UIUX设计增强 获取参考方向
2. 确定风格后 → /theme-factory 选/生成主题色板
3. 静态视觉产出(海报/PDF)？ → /canvas-design 创作
4. 原型阶段缺图？ → /placeholder-image 获取占位图
规则：灵感(1)→风格(2)→产出(3/4)。已有明确风格跳过1-2。
```

---

## @frontend-dev — 前端开发者

> 前端页面编码、组件开发、路由分析、性能优化。React/Vue/Taro/小程序等前端工程。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/frontend-design` | 全局 | 生产级前端界面代码生成。拒绝AI通用美学，强调大胆设计方向(极简/野兽派/复古未来等)。输出可运行的HTML/CSS/JS或React/Vue代码 |
| `/web-artifacts-builder` | 全局 | 复杂多组件Web工件构建。React18+TS+Vite+Tailwind+shadcn/ui，40+预装组件。适用于需要状态管理/路由的复杂前端artifact |
| `/ui-ux-pro-max` | 全局 | UI/UX设计智能库。50+样式/97色板/57字体配对/99条UX准则，覆盖9个技术栈。--design-system一键生成完整设计方案 |
| `/analyze-routes` | 全局 | 路由&接口分析。自动扫描前端路由结构/接口调用/Mock数据使用，生成结构化分析报告。支持web/admin/h5/miniapp四种项目类型 |
| `/fe-optimize` | 全局 | 前端性能优化与规范检测。6维检测：接口调用/缓存策略/渲染性能/打包体积/网络传输/全面检测。从代码提取实际模式对照最佳实践，输出可落地优化方案 |

**触发词**: 前端开发、写页面、React、Vue、Taro、小程序、组件开发、tsx、css、路由分析、前端优化、性能优化

**内部执行策略**:
```
1. 新项目/新页面？ → /analyze-routes 先扫描现有路由结构
2. 需要设计方案？ → /ui-ux-pro-max 生成设计系统(色板/字体/间距)
3. 写页面代码 → 简单页面直接写，复杂多组件用 /web-artifacts-builder，需要高设计感用 /frontend-design
4. 开发完成后 → /fe-optimize 检测性能+规范(6维扫描)
规则：了解现状(1)→设计(2)→编码(3)→优化(4)。改bug/小调整直接跳到3。
```

---

## @backend-dev — 后端开发者

> 后端接口编码、Service实现、数据模型、API调试。.NET/Node/Python等后端工程。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/debug` | 项目级 | 从当前项目 `.claude/commands/debug.md` 读取。项目开发环境启动/重启，含项目专属的端口、路径、启动脚本配置 |
| `/context-memory` | 全局 | 上下文记忆文档生成。提取当前对话的目标/操作/状态/决策/偏好，生成结构化markdown，复制到新对话即可恢复工作状态 |

**触发词**: 后端开发、写接口、API、Service、Model、数据库、.NET、C#、Node、启动服务、调试后端

**内部执行策略**:
```
1. 需要启动/重启服务？ → /debug 管理开发环境
2. 编码实现 → 直接写代码（读参考文件→模仿→实现）
3. 长对话/复杂任务中途？ → /context-memory 保存上下文快照
规则：编码为主，skill 为辅。大部分后端任务直接写代码，skill 只在环境管理和上下文保存时用。
```

---

## @toolsmith — 工具匠

> Skill/MCP/插件等开发工具的创建与维护。需要造轮子时激活。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/skill-creator` | 全局 | Skill开发框架。6步流程：理解需求→规划资源→init_skill.py初始化→编辑SKILL.md+资源→package_skill.py打包→迭代。渐进式披露原则 |
| `/skill-from-masters` | 全局 | 专家方法论驱动的Skill创建。在skill-creator之前使用，从领域专家中发现并融入成熟方法论框架。含子技能：search-skill(市场搜索)/skill-from-github(从GitHub学习)/skill-from-notebook(从文档提取方法论) |
| `/mcp-builder` | 全局 | MCP Server开发指南。4阶段：深度调研→实现→审查测试(MCP Inspector)→评估。推荐TypeScript+Streamable HTTP，含Python/Node双语言参考 |
| `/init-project` | 全局 | 项目定制初始化。批量更新.claude/commands/下所有skills中的项目特定信息(名称/路径/端口/服务器)，从旧项目迁移skills到新项目时一键替换 |
| `/coding-agent` | 全局 | 后台进程控制(openclaw/branexp)。通过后台进程启动Codex CLI/Claude Code/OpenCode，实现多agent并行编码、任务分发、进度监控 |

**触发词**: 创建skill、新技能、MCP Server、造工具、写插件、开发MCP、初始化项目、迁移skills

**内部执行策略**:
```
1. 创建新 Skill？ → /skill-from-masters 先找领域专家方法论
2. 方法论确定后 → /skill-creator 按框架生成 SKILL.md + 资源
3. 创建 MCP Server？ → /mcp-builder 按4阶段开发
4. 迁移到新项目？ → /init-project 批量替换项目信息
规则：造 Skill 走1→2，造 MCP 走3，迁移走4。不混用。
```

---

## @reviewer — 审查员

> Code Review、规范检查、方案评审、安全审计、代码质量监理。代码完成后激活。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/solution-evaluator` | 全局 | (共享@researcher) 技术方案评估，也可用于代码方案审查 |
| `/fe-optimize` | 全局 | (共享@frontend-dev) 前端规范检测维度可用于Review |
| `/audit-project` | 全局 | 最强项目审查(avifenesh/agentsys)。最多10个专项agent并行（代码质量/安全/性能/测试/架构/数据库/API/前端/后端/DevOps），基于技术栈自动选审查员，按严重度分级(critical/high/medium/low)，自动修复非误报，迭代直到全部通过 |
| `/code-review` | 全局 | Sentry团队code review skill(getsentry/skills)。标准化审查指南+检查清单，确保跨团队一致的代码质量标准 |
| `/audit-context-building` | 全局 | 超细粒度代码分析(trailofbits/skills)。审计前先通过ultra-granular分析建立完整架构理解，为安全审查提供深度上下文 |
| `/variant-analysis` | 全局 | 变体漏洞搜索(trailofbits/skills)。发现一个bug后，基于模式分析在全代码库搜索结构相似的代码，定位同类潜在漏洞 |
| `/sharp-edges` | 全局 | 危险API识别(trailofbits/skills)。定位易出错的API、危险配置项、常见导致安全问题的设计模式 |
| `/ClawSec` | 全局 | 安全套件(prompt-security/clawsec)。5个子skill：CVE每日轮询(NVD)/漂移检测+文件完整性守护+自动恢复/自动每日安全审计/社区威胁情报/SHA256校验安装器 |
| `/deslop` | 全局 | 代码质量监理(avifenesh/agentsys)。3阶段检测：Phase1正则(HIGH)—console.log/TODO/空catch；Phase2多轮分析(MEDIUM)—过度注释/死代码/stub；Phase3 CLI工具(LOW)—jscpd/pylint/clippy。支持JS/Python/Rust/Go/Java |
| `/sync-docs` | 全局 | 文档同步检测(avifenesh/agentsys)。识别文件移动/重命名导致的引用断裂、版本不匹配、CHANGELOG缺失。自动修复安全更新或标记需人工审查 |
| `/drift-detect` | 全局 | 计划vs代码漂移检测(avifenesh/agentsys)。语义匹配(非字符串)，检测文档写了但没实现+实现了但没文档的部分。比多agent方案省77% token |
| `/oracle` | 全局 | 第二模型交叉验证(steipete/oracle)。打包prompt+文件发给Codex/GPT-5 Pro做交叉验证、debug、重构审查、设计检查。API或浏览器模式 |

**触发词**: review、审查、检查代码、安全审计、规范检查、代码质量、漏洞扫描、漂移检测、文档同步、代码清理

**内部执行策略**:
```
1. 全面项目审查？ → /audit-project 启动多agent并行审查
2. 审查前需要理解架构？ → /audit-context-building 先建立上下文
3. 前端代码？ → /fe-optimize 跑6维规范检测
4. 方案/架构层面？ → /solution-evaluator 评估缺陷+替代方案
5. 安全审计？ → /sharp-edges 识别危险API + /ClawSec CVE监控
6. 发现bug后？ → /variant-analysis 全库搜索同类漏洞
7. 代码清理？ → /deslop 3阶段检测debug语句/死代码/占位文本
8. 文档与代码不同步？ → /sync-docs 检测断裂引用 + /drift-detect 语义漂移
规则：按审查目标组合，不必全部调用。快速review只用4，全面审计用1→2→5→6。
```

---

## @tester — 测试员

> 自动化测试、功能验证、浏览器测试、性能调查。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/webapp-testing` | 全局 | Playwright浏览器自动化测试。含with_server.py管理服务器生命周期，侦察-行动模式(先截图/检查DOM→发现选择器→执行操作)。必须等networkidle后再操作DOM |
| `/perf` | 全局 | 最全面性能调查(avifenesh/agentsys)。10阶段结构化方法论：建立基线(60秒最低)→约束测试(二分查找失败阈值)→生成假设(最多5个+置信度)→语言专属profiling(CPU/内存/JFR/cProfile/pprof)→单变量实验→证据日志→最终决策 |
| `/unit-testing-test-generate` | 全局 | 多语言单元测试生成(sickn33/antigravity)。支持pytest/Jest/JUnit/Go test，自动检测覆盖率缺口，生成边界条件+异常路径测试，含mock策略和fixture管理 |
| `/javascript-testing-patterns` | 全局 | JS/TS测试全套策略(sickn33/antigravity)。Jest/Vitest/Testing Library，覆盖单元测试/集成测试/E2E，含mocking/fixtures/TDD-BDD工作流 |
| `/e2e-testing-patterns` | 全局 | E2E测试模式(sickn33/antigravity)。Playwright+Cypress全套：flaky test调试、跨浏览器兼容、无障碍验证、CI集成策略 |
| `/verification-before-completion` | 全局 | 完成前验证(obra/superpowers)。"证据先于声明"原则——声称完成/修复/通过前，必须运行验证命令并读取完整输出。防止虚假完成声明 |
| `/test-fixing` | 全局 | 智能测试修复(sickn33/antigravity)。运行测试→按错误类型智能分组→按依赖顺序逐组修复→验证全绿。避免逐个修复的低效模式 |

**触发词**: 测试、验证、跑测试、截图对比、E2E、Playwright、浏览器测试、性能调查、profiling、基准测试、单元测试、Jest、覆盖率、TDD执行

**内部执行策略**:
```
1. 需要生成单元测试？ → /unit-testing-test-generate 自动生成+覆盖率缺口检测
2. JS/TS前端测试？ → /javascript-testing-patterns 提供Jest/Vitest策略
3. E2E浏览器测试？ → /e2e-testing-patterns 定策略 + /webapp-testing 执行
4. 性能问题？ → /perf 10阶段结构化调查
5. 任何任务完成前 → /verification-before-completion 强制验证
6. 测试批量失败？ → /test-fixing 智能分组+依赖顺序修复
规则：生成(1-2)→执行(3-4)→验证(5)→失败修复(6)。验证步骤不可跳过。
```

---

## @devops — 运维员

> 部署、诊断、服务器操作。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/deploy` | 项目级 | 从当前项目 `.claude/commands/deploy.md` 读取。项目一键部署，含项目专属的服务器、Docker、端口等部署配置。生产环境必须请示用户 |
| `/deployment-diagnosis` | 全局 | 部署诊断。系统化分析部署状态：服务器资源/Docker容器/数据库连接/前后端配置/配置一致性检查。支持full/quick/server/config四种模式 |

**触发词**: 部署、上线、发布、Docker、CI/CD、服务器、deploy、诊断、排查

---

## @writer — 文档员

> 文档撰写、汇报生成、内部通讯。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/work-report` | 全局 | Git提交→结构化工作汇报。6阶段流程，适配4种对象语气(领导/客户/同事/监理)，数据驱动不编不猜，量化表达 |
| `/doc-coauthoring` | 全局 | 结构化文档协作。3阶段：上下文收集→逐节精炼→读者测试(子agent模拟新读者验证可理解性) |
| `/internal-comms` | 全局 | 企业内部通讯模板库。支持3P更新/新闻稿/FAQ/状态报告/领导汇报/项目更新/事故报告 |
| `/change-log` | 全局 | 代码修改记录(配合/commit)。基于git diff生成结构化变更文档，按前端/后端/数据库/配置分类，含代码片段+行号链接，支持增量追加，⚠️标记数据库和脚本变更 |

**触发词**: 写文档、周报、月报、汇报、技术文档、提案、规范、内部通讯、FAQ、记录修改、变更日志

---

## @remote-assistant — 远程助手

> 远程连接使用happy这个github的开源项目 远程移动端cc的场景下的 文件查找、飞书消息发送、截图分享。通过 Happy/远程桌面 操作时激活。

| Skill | 来源 | 能力详情 |
|-------|------|---------|
| `/file-query` | 全局 | Everything引擎本地文件极速搜索。自然语言→Everything语法翻译、tree风格输出(序号+大小+时间)、分页、时间/大小/类型过滤。结果可通过序号与/share-feishu联动 |
| `/share-feishu` | 全局 | 飞书群消息发送。3种模式：card/file/both。目录自动压缩zip，支持/file-query序号联动 |
| `/camera-feishu` | 全局 | 相机拍照→飞书群发送 |
| `/screenshot-feishu` | 全局 | 屏幕截图→飞书群发送 |

**触发词**: 找文件、本地搜索、发飞书、分享、截图发群、拍照发群、发文件到群

---

## 角色协作矩阵

> 编排器根据工作流自动组合角色，不需要用户指定。

| 工作流 | 角色链 |
|--------|--------|
| 快速流(前端) | @frontend-dev → @tester |
| 快速流(后端) | @backend-dev → @tester |
| 标准流(前端) | @researcher → @frontend-dev → @reviewer → @tester |
| 标准流(后端) | @researcher → @backend-dev → @reviewer → @tester |
| 标准流(全栈) | @researcher → (@frontend-dev + @backend-dev 并行) → @reviewer → @tester |
| 完整流 | @researcher → @architect → @designer → (@frontend-dev + @backend-dev 并行) → (@reviewer + @tester 并行) → @writer |
| 部署流 | @devops |
| 文档流 | @researcher → @writer |
| 工具流 | @researcher → @toolsmith → @tester |
| 分享流 | @remote-assistant |
| TDD流(后端) | @architect(定测试策略) → @tester(Red) → @backend-dev(Green) → @tester(Refactor验证) → @reviewer |
| TDD流(前端) | @architect(定测试策略) → @tester(Red) → @frontend-dev(Green) → @tester(Refactor验证) → @reviewer |
| TDD流(全栈) | @architect(定测试策略) → (@tester+@backend-dev 交替Red-Green) + (@tester+@frontend-dev 交替Red-Green) → @reviewer |

---

## 角色 → 模型推荐

| 角色 | 推荐模型 | 原因 |
|------|---------|------|
| @researcher | haiku | 搜索查询，速度优先 |
| @architect | opus | 架构决策需要深度推理 |
| @designer | sonnet | 视觉创意+代码平衡 |
| @frontend-dev | sonnet | 前端代码生成主力 |
| @backend-dev | sonnet | 后端逻辑实现主力 |
| @toolsmith | opus | 工具/框架设计需要深度理解 |
| @reviewer | opus | 审查需要全局视角 |
| @tester | haiku | 测试执行，速度优先 |
| @devops | sonnet | 部署脚本+环境配置 |
| @writer | sonnet | 文档质量+效率平衡 |
| @remote-assistant | haiku | 文件查找+消息转发，速度优先 |

---

## 更新规则

执行 `/skill-orchestrator update` 时：
1. 扫描两层来源目录（排除 skill-orchestrator 自身）
2. 读取每个 skill 的 SKILL.md/.md 提取 name + description + 核心能力
3. 按角色触发词自动归类（无法归类 → @backend-dev）
4. 已删除的 Skill → 从注册表移除
5. 不覆盖手动编辑的能力详情
6. 输出变更摘要
